{
  "type": "action",
  "version": "1.0.0",
  "name": "Webhook on User Created",
  "description": "Send an HTTP POST webhook whenever a new user identity is created. Useful for syncing users to external systems like CRMs, mailing lists, or provisioning tools.",
  "variables": {
    "webhook_url": {
      "type": "string",
      "description": "URL to send the webhook POST to"
    },
    "secret": {
      "type": "string",
      "description": "Shared secret for HMAC-SHA256 signature (X-Webhook-Signature header)",
      "sensitive": true
    }
  },
  "payload": {
    "display_name": "Webhook: User Created",
    "hook": "on_event",
    "action_type": "webhook",
    "trigger": "event.type == 'identity.created'",
    "config": {
      "url": "{{webhook_url}}",
      "method": "POST",
      "secret": "{{secret}}",
      "headers": {
        "Content-Type": "application/json"
      },
      "body_template": "{\"event\": \"{{event.type}}\", \"entity_id\": \"{{event.entity_id}}\", \"timestamp\": \"{{event.created_at}}\"}"
    },
    "priority": 50,
    "enabled": true,
    "fail_open": true,
    "timeout_ms": 10000
  }
}
